# Netlify configuration — FortitudeFX

[functions]
  directory = "netlify/functions"

# ── Page redirects ────────────────────────────────────────────────────────────
[[redirects]]
  from = "/"
  to   = "/index.html"
  status = 200

[[redirects]]
  from = "/bootcamp"
  to   = "/bootcamp.html"
  status = 200

[[redirects]]
  from = "/vip-discord"
  to   = "/vipdiscord.html"
  status = 200

[[redirects]]
  from = "/waitlist"
  to   = "/waitlist.html"
  status = 200

[[redirects]]
  from = "/privacy"
  to   = "/privacy.html"
  status = 200

# ── Security headers ──────────────────────────────────────────────────────────
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
